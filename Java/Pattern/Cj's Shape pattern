//Creating a Package for my class name javaFistAct
package oopFirstAct;

//Created a public class named javaFirstAct where I can access it anywhere in my different package
public class javaFirstAct {
	/**Created a public(access specifier) static(keyword) void(return type)
	 * which allows the main(method name) on String[] args(array of string types) which
	 * stores java command-line arguments. */
	public static void main(String[] args) {
		
		//Using a non-primitive data type
		//A string named "Name" and "Section" that has a char value 
		String Name = "Christian Jay Baguio";
		String Section = "DS2A";
		
		//printing by passing the Name string and add it to section string
		System.out.println(Name + "|" + Section);
		
		//This is for print shape diamond
		System.out.println("*");
		System.out.println("**");
		System.out.println("***");
		System.out.println("****");
		System.out.println("*****");
		System.out.println("****");
		System.out.println("***");
		System.out.println("**");
		System.out.println("*\n");
		
		//This is for print shape circle
		System.out.println("***");
		System.out.println("****");
		System.out.println("*****");
		System.out.println("****");
		System.out.println("***\n"); 
		
		//This is for the Square Shape
		System.out.println("**********");
		System.out.println("**********");
		System.out.println("**********");
		System.out.println("**********");
		System.out.println("**********\n");
		
		
		/**I'm using a for loop so that the repetition is control and it allows me to loop that
		 * is need to be executed a specific number of times*/
		
		//This is for the Rectangle Shape
		//this area is conditional is the rows like the amount of rows you want to put
		for(int r = 0; r <= 3; r++) { /**if the int is less than or equal to 3 the int will
			move to unary code which is the increment op where i use post-increment so that it will add plus one to the int
			and if the r satisfy the 3 which is rows, it will break automatically*/
			
			//This conditional area is for the column part 
			for(int s = 0; s < 20; s++) { 
				//for each star I just print it and not print a new line
				System.out.print("*");
			}
			//for every row I print it in a separate line
			System.out.println();
		}
		
		
	}

}
